name: Build AntiSpam IPA
on: [push]

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Xcode version
        run: xcodebuild -version

      - name: List schemes
        run: xcodebuild -project AntiSpam.xcodeproj -list

      - name: Build
        run: |
          xcodebuild clean build \
            -project AntiSpam.xcodeproj \
            -scheme AntiSpam \
            -configuration Release \
            -sdk iphoneos \
            CODE_SIGN_IDENTITY="-" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            DEVELOPMENT_TEAM="" \
            PROVISIONING_PROFILE="" \
            PROVISIONING_PROFILE_SPECIFIER="" \
            -derivedDataPath build

      - name: Package IPA
        run: |
          APP=$(find build/Build/Products/Release-iphoneos -name "AntiSpam.app" | head -1)
          echo "App trouvée : $APP"
          mkdir -p Payload
          cp -r "$APP" Payload/
          zip -r AntiSpam.ipa Payload
          ls -lh AntiSpam.ipa

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: AntiSpam-IPA
          path: AntiSpam.ipa
